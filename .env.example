# Environment Variables Configuration Example
# Copy this file to .env, remove the comments, and adjust the values as needed

# Application Settings
HOST_PORT=8000        # Port for the Python application
NGINX_PORT=80         # Port for Nginx reverse proxy (used to access the server)

# Optional Settings (Defaults shown)
# HOST_IP=0.0.0.0     # Interface to bind to
# MAX_WORKERS=4       # Number of Gunicorn workers
# TIMEOUT=120         # Request timeout in seconds
